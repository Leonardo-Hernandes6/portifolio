import { AboutMe } from "../AboutMe/about-me";
import { ScrollViewContainer } from "./style";

export const ScrollView = () => {
  return (
    <ScrollViewContainer>
      <AboutMe />
    </ScrollViewContainer>
  );
};
